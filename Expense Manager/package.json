{
  "name": "expense-manager",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf node_modules __lwr_cache__ site",
    "dev": "lwr dev",
    "dev:compat": "lwr dev --mode compat",
    "build": "lwr build --clean",
    "build:prod-compat": "lwr build --clean --mode prod-compat",
    "start": "lwr start",
    "stage": "yarn build && yarn start",
    "stage:prod-compat": "yarn build:prod-compat && yarn start"
  },
  "dependencies": {
    "@lwrjs/config": "^0.10.12",
    "@lwrjs/label-module-provider": "^0.10.12",
    "json-server": "^0.17.4",
    "lightning-base-components": "^1.19.11-alpha",
    "lwc": "3.0.0",
    "lwr": "^0.9.5"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "volta": {
    "node": "18.16.1"
  }
}
